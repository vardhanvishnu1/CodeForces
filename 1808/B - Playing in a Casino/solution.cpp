#include <iostream>#include <vector>#include <algorithm> using namespace std; /** * The total sum is the sum of differences for each of the 'm' dimensions independently. * For each dimension, we sort the values to calculate the sum of absolute differences in O(n log n). */void solve() {    int n, m;    if (!(cin >> n >> m)) return;     // Use a 2D vector where cards[m][n] stores the i-th number of every card    // This allows us to process each dimension (column) easily.    vector<vector<long long>> columns(m, vector<long long>(n));     for (int i = 0; i < n; ++i) {        for (int j = 0; j < m; ++j) {            cin >> columns[j][i];        }    }     long long total_winnings = 0;     for (int j = 0; j < m; ++j) {        // Sort the numbers in the current dimension        sort(columns[j].begin(), columns[j].end());         // Calculate sum of differences for this dimension        // Sum of |x_i - x_j| = sum_{i=0}^{n-1} (x_i * (2*i - n + 1))        for (int i = 0; i < n; ++i) {            total_winnings += columns[j][i] * (2LL * i - n + 1);        }    }     cout << total_winnings << "\n";} int main() {    ios_base::sync_with_stdio(false);    cin.tie(NULL);     int t;    cin >> t;    while (t--) {        solve();    }    return 0;}