#include <bits/stdc++.h>using namespace std; typedef long long ll;int MOD = 1e9 + 7;  // matrix rotate by 90 degree => a[i][j] = a[j][n-i-1] ll rs(ll l,ll r) {    if(l>r){        return 0;    }    // Using the Arithmetic Series formula: (n / 2) * (first + last)    return (l + r) * (r - l + 1) / 2;} ll unq(vector<ll> &a){    // prints only the unique elements    // removes the contigous duplicates and returns the new size    ll n = unique(a.begin(), a.end()) - a.begin();    return n;} ll __lcm(ll a, ll b){    return (a / __gcd(a, b)) * b;} void fast_io(){    ios::sync_with_stdio(false);    cin.tie(NULL);    cout.tie(NULL);} int main() {	// your code goes here// ll t;// cin>>t;// while(t--){    // 1 1 1 6 6 8 8 10 10    // 1 1 4    // 1 1     ll n,k,x;cin>>n>>k>>x;    vector<ll>a(n);    for(ll i=0;i<n;i++) cin>>a[i];    sort(a.begin(),a.end());    ll ans = 1;    vector<ll>gaps;    for(ll i=1;i<n;i++){        if(a[i]-a[i-1]>x){            gaps.push_back(((a[i]-a[i-1]-1)/x));        }    }    sort(gaps.begin(),gaps.end());    for(ll i=0;i<gaps.size();i++){        if(k>=gaps[i]){            k-=gaps[i];        }        else{            ans++;        }    }    cout<<ans<<endl;// }}