#include <bits/stdc++.h> using namespace std; #define ll long long#define io                            \    ios_base::sync_with_stdio(false); \    cin.tie(NULL);                    \    cout.tie(NULL) ll least_common_factor(const vector<ll>& a) {    ll n = a.size();    ll min_val = *min_element(a.begin(), a.end());    for (ll i = 2; i <= min_val; ++i) {        bool is_common_factor = true;        for (ll j = 0; j < n; ++j) {            if (a[j] % i != 0) {                is_common_factor = false;                break;            }        }        if (is_common_factor)            return i;     }    return -1; } bool customSort(const pair<long long, long long>& a, const pair<long long, long long>& b) {    if (a.first != b.first)        return a.first < b.first;    return a.second > b.second;    } ll xor_upto(ll n) {    if(n % 4 == 0) return n;    if(n % 4 == 1) return 1;    if(n % 4 == 2) return n + 1;    return 0;} ll gcd(ll a, ll b) {    while(b != 0) {        ll temp = b;        b = a % b;        a = temp;    }    return a;}ll lcm(ll a, ll b) {    return (a / gcd(a, b)) * b; } ll maxSubArray(vector<ll>& nums) {        ll sum =0;        ll ans = INT_MIN;        ll start = 0;        ll ansStart,ansEnd = 0;        for(ll i=0;i<nums.size();i++){            if(sum==0) start = i;            sum+=nums[i];            if(ans<sum) {                ans = sum;                ansStart = start; ansEnd = i;            }            if(sum<0) {sum=0;}        }        cout<<ansStart<<" "<<ansEnd<<endl;        return ans;} bool isPowerOfTwo(ll x) {    return x > 0 && (x & (x - 1)) == 0;}   int main(){    io;    ll t;    cin>>t;    while(t--){        ll n;        cin>>n;        vector<ll>w(n);        for(ll i=0;i<n;i++) cin>>w[i];       ll l=0;       ll r= n-1;       ll suml=w[0];       ll sumr=w[n-1];       ll ans =0;       while(l<r){           if(suml==sumr) ans = max(ans,n-r+l+1);           if(suml<=sumr){               l+=1;               suml+=w[l];           }           else if(suml>sumr) {               r-=1;               sumr+=w[r];           }       }       cout<<ans<<endl;    }}